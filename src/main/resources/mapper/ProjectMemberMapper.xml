<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.jjxy.researchmanagementsystem.mapper.ProjectMemberMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO research_project_member(project_id, person_id, duty, join_date, remark)
        VALUES (#{projectId}, #{personId}, #{duty}, #{joinDate}, #{remark})
    </insert>

    <delete id="deleteById">
        DELETE FROM research_project_member WHERE id = #{id}
    </delete>

    <select id="listByProjectId" resultType="edu.jjxy.researchmanagementsystem.entity.ProjectMember">
        SELECT id, project_id, person_id, duty, join_date, remark
        FROM research_project_member
        WHERE project_id = #{projectId}
        ORDER BY id ASC
    </select>

</mapper>
